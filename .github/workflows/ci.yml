name: ci

# The full test suite — unit, characterization goldens, REST-API contract,
# and architecture guards — on every PR and push to main. The cross-backend
# SPARQL parity smoke keeps its own path-filtered workflow
# (cross-backend-smoke.yml); it also runs here as part of the suite.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  tests:
    name: pytest (full suite)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install platform + API deps
        run: |
          pip install -r requirements.txt -r apps/api/requirements.txt
          pip install pytest pytest-cov httpx
      # Coverage ratchet: the floor records where backend/ coverage stands
      # (measured total minus 2 points of headroom), so it can only go up.
      # Raise it when new tests land; never chase it with filler tests.
      - name: Run test suite
        run: pytest -v --cov=backend --cov-fail-under=51
