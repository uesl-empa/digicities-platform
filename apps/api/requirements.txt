# Digicities REST API (apps/api) — HTTP seam over the in-process backend.
# Installed on top of the platform's own requirements (backend.* + its deps).
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart uploads for the /replica endpoint (next)
# Metadata database (workspace catalog + phase-2 users/ACL). Optional at runtime:
# with no DATABASE_URL the API falls back to the filesystem registry.
sqlalchemy>=2.0
psycopg2-binary>=2.9
alembic>=1.13
# Phase 2: local accounts (JWT + password hashing).
pyjwt>=2.8
bcrypt>=4.1
